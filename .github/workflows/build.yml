name: Build plugin

on:
  push:
    branches: [main]
    tags: ["v*"]
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build frontend
        run: npm run build

      - name: Check backend syntax
        run: python3 -m py_compile main.py py_modules/gdrive.py

      - name: Assemble plugin zip
        run: |
          mkdir -p out/decky-backup/dist out/decky-backup/py_modules
          cp plugin.json package.json main.py README.md LICENSE out/decky-backup/
          cp dist/index.js out/decky-backup/dist/
          cp py_modules/*.py out/decky-backup/py_modules/
          cd out && zip -r decky-backup.zip decky-backup

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: decky-backup
          path: out/decky-backup.zip

      - name: Attach to release
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v2
        with:
          files: out/decky-backup.zip
          generate_release_notes: true
